import {
  useReactTable,
  getCoreRowModel,
  getPaginationRowModel,
  getSortedRowModel,
  createColumnHelper,
} from "@tanstack/react-table";
import { useFetchDomains } from "@/hooks/useFetchDomains";
import { useFetchTokenizedDomains } from "@/hooks/useFetchDomains";

const DomainTable = () => {
  const useFetchDo;
  const columnHelper = createColumnHelper();

  return <h1>test</h1>;
};
